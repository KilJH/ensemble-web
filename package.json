{
  "name": "ensemble-web",
  "version": "0.1.0",
  "private": true,
  "description": "앙상블 - 음악 동호인을 위한 워크스페이스 기반 SaaS",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter @ensemble/web dev",
    "dev:api": "pnpm --filter @ensemble/api dev",
    "build": "turbo run build",
    "build:web": "pnpm --filter @ensemble/web build",
    "build:api": "pnpm --filter @ensemble/api build",
    "lint": "turbo run lint",
    "lint:web": "pnpm --filter @ensemble/web lint",
    "lint:api": "pnpm --filter @ensemble/api lint",
    "typecheck": "turbo run typecheck",
    "typecheck:web": "pnpm --filter @ensemble/web typecheck",
    "typecheck:api": "pnpm --filter @ensemble/api typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,mdx,css,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,mdx,css,yaml,yml}\"",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "eslint": "^8.56.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.1",
    "turbo": "^1.11.3",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
