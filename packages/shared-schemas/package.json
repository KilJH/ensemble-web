{
  "name": "@ensemble/shared-schemas",
  "version": "0.1.0",
  "private": true,
  "description": "Shared validation schemas for Ensemble monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@ensemble/tsconfig": "workspace:*",
    "@ensemble/types": "workspace:*",
    "typescript": "^5.3.3"
  }
}
